# Read the Docs configuration file
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.10"   #(안정 버전)

sphinx:
  configuration: docs/source/conf.py   #실제 경로!

python:
  install:
    - requirements: docs/requirements.txt
